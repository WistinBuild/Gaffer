[package]
name = "gaffer-nft"
version = "0.1.0"
description = "Gaffer squad NFTs — soulbound 5-player squads (Solana port of GafferNFT.sol)"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "gaffer_nft"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.31.1"
